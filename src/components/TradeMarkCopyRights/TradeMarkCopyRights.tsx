import React, { FC } from 'react'
import { currentYear } from '../../constant'

const TradeMarkCopyRights: FC = () => {

    return (
        <div>© {currentYear} ERELA. Tous droits réservés.</div>
    )
}

export default TradeMarkCopyRights